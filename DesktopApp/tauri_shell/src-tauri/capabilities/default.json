{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for the MediaPipeline shell window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
