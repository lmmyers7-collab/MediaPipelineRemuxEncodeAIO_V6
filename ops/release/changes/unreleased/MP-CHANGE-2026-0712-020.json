{
  "id": "MP-CHANGE-2026-0712-020",
  "title": "Close residual CodeQL CORS fallback",
  "version_target": "2026.06.04.001",
  "status": "complete",
  "type": "bugfix",
  "risk_level": "medium",
  "date_started": "2026-07-12",
  "date_completed": "2026-07-12",
  "summary": "Make the Local API handler's resolver-absent CORS fallback server-authored instead of reflecting an untrusted request Origin.",
  "reason": "The post-merge CodeQL scan kept alert 281 open because the generic handler fallback still provided a request-header-to-response-header dataflow when an owner did not implement the canonical CORS resolver.",
  "affected_areas": [
    "desktop-local-api",
    "security",
    "tests"
  ],
  "behavior_before": "Production LocalApiServer owners canonicalized allowed origins, but a resolver-less handler owner reflected the raw Origin request header into CORS response headers.",
  "behavior_after": "Production owners retain canonical allowlisted origins; resolver-less owners use the fixed loopback origin and never reflect request input.",
  "files_touched": [
    "ops/release/changes/unreleased/MP-CHANGE-2026-0712-020.json",
    "docs/generated/summaries/src/mediapipeline/desktop/api/handler.py.md",
    "docs/generated/summaries/tests/python/desktop/test_application_facade_local_api_http.py.md",
    "docs/generated/DUPLICATE_TEST_NAMES.md",
    "src/mediapipeline/desktop/api/handler.py",
    "tests/python/desktop/test_application_facade_local_api_http.py"
  ],
  "tests_added": [
    "Regression coverage proves a handler owner without a CORS resolver ignores a CR/LF-bearing request Origin and returns the fixed loopback fallback."
  ],
  "manual_validation": [
    "PASS: focused Local API policy/HTTP suite: 31 tests.",
    "PASS: Ruff across src and tests.",
    "PASS: full bundled-Python discovery: 3,142 tests, one documented skip.",
    "PASS: strict source release self-test with required tests, WebView reliability, tool integration, and end-to-end smoke.",
    "PASS: PR 38 CodeQL run 29220542137 completed successfully and the pull-request merge ref has zero open code-scanning alerts; alert 281 was fixed without dismissal."
  ],
  "rollback_plan": "Revert the handler fallback and regression test, then rerun the focused Local API security and HTTP tests.",
  "related_changes": [
    "MP-CHANGE-2026-0712-005"
  ],
  "notes": "Generated as the next repository packet, then assigned 020 to avoid colliding with known concurrent untracked packets 014 through 019 in the user's primary checkout. No GitHub alert was dismissed and no security query or gate was weakened. The first aggregate discovery exposed missing bundled-tool junctions in the isolated worktree; after linking the canonical bundled PowerShell and media tools, the two probe tests passed in isolation and the full discovery passed."
}
