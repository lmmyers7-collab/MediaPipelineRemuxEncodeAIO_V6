# File lifecycle map

Generated by `scripts/dev/generate_lifecycle_map.py` from `app/contracts/lifecycle.py`. Do not hand-edit.

Schema version: `file_lifecycle.v1`

This map describes the current V6 file lifecycle and explicitly marks desired-but-absent
folder concepts so AI agents do not infer runtime states that the pipeline does not own.

## Source references

- `Docs/operator/NO_TOUCH_BOUNDARY_REGISTER.md`
- `Docs/inventories/RUNTIME_ARTIFACT_INVENTORY.md`
- `Docs/inventories/STATE_FILE_SCHEMA_REFERENCE.md`
- `Docs/architecture/LOCAL_API_EVIDENCE_MUTATION_MATRIX.md`
- `Docs/architecture/REPAIR_RECONCILE_MUTATION_CONTRACT.md`

## States

| State | Implemented | Owner | Description | Validation |
|---|---|---|---|---|
| `source_library` | yes | Pipeline source discovery and scratch copy | Operator media roots are treated as immutable inputs. | `media-policy-release-real-media` |
| `incoming_candidate` | no | Not currently implemented as a dedicated folder | No canonical incoming/ intake_pending folder state exists in V6 today. | `docs-and-contract-checks` |
| `scratch_copy` | yes | engine scratch-copy and disk modules | Source media is copied to local scratch before downstream mutation-capable work. | `media-policy-release-real-media` |
| `processing` | yes | Pipeline engine, FFmpeg, subtitle, and audio modules | Scratch media is probed, routed, remuxed, encoded, and sidecars may be produced. | `media-policy-release-real-media` |
| `local_output` | yes | Pipeline publish preparation | Completed local media output awaits final publish or pending-publish parking. | `settings-pending-publish-diagnostics` |
| `pending_publish` | yes | engine publish pending-transaction and drain modules | Output is parked when final-root publish is unsafe or deferred. | `settings-pending-publish-diagnostics` |
| `final_output` | yes | Publish completion and completed manifest modules | Validated media output is visible in the final library root. | `settings-pending-publish-diagnostics` |
| `failure_review` | yes | Failure state and desktop failure cleanup services | Failure markers and reports preserve evidence for operator review. | `settings-pending-publish-diagnostics` |
| `ready_archive_quarantine_trash` | no | Not currently implemented as dedicated folders | The audit terms exist as desired lifecycle concepts but are not canonical V6 runtime folders today. | `docs-and-contract-checks` |

## State details

### Source library

- **State id:** `source_library`
- **Implemented:** yes
- **Owner:** Pipeline source discovery and scratch copy
- **Allowed operations:** `read`, `probe metadata`, `copy to scratch`
- **Forbidden operations:** `delete by default`, `overwrite`, `in-place transcode`, `publish from source`
- **Artifacts:** `source path`, `source size`, `source mtime`, `source identity fields`
- **Recovery rule:** Source media must remain available for retry unless an explicit safe-delete policy is intentionally enabled.
- **Cleanup rule:** No cleanup is allowed against source roots by default.
- **Validation rung:** `media-policy-release-real-media`

### Incoming candidate

- **State id:** `incoming_candidate`
- **Implemented:** no
- **Owner:** Not currently implemented as a dedicated folder
- **Allowed operations:** `document as absent`
- **Forbidden operations:** `treat as a live queue state without adding code and tests`
- **Artifacts:** `n/a`
- **Recovery rule:** Use the existing source discovery and scratch-copy lifecycle until a dedicated intake state is added.
- **Cleanup rule:** No cleanup rule because the state is not implemented.
- **Validation rung:** `docs-and-contract-checks`

### Scratch copy

- **State id:** `scratch_copy`
- **Implemented:** yes
- **Owner:** engine scratch-copy and disk modules
- **Allowed operations:** `copy from source`, `hash or size verification`, `consume for probe/transcode`
- **Forbidden operations:** `expose partial copy as ready`, `overwrite unrelated scratch files`, `delete source on copy failure`
- **Artifacts:** `local scratch path`, `copy telemetry`, `sha256 or size evidence`
- **Recovery rule:** Failed or partial scratch copies remain retryable and should not affect source media.
- **Cleanup rule:** Stale partial scratch artifacts are eligible for guarded cleanup after retention.
- **Validation rung:** `media-policy-release-real-media`

### Processing

- **State id:** `processing`
- **Implemented:** yes
- **Owner:** Pipeline engine, FFmpeg, subtitle, and audio modules
- **Allowed operations:** `read scratch`, `write temp outputs`, `write sidecars`, `write logs`
- **Forbidden operations:** `publish unverified partial output`, `silently drop subtitle/OCR failures`, `mutate source`
- **Artifacts:** `FFmpeg logs`, `progress state`, `failure markers`, `sidecar outputs`
- **Recovery rule:** Failures route to failure markers/review and preserve enough evidence for operator recovery.
- **Cleanup rule:** Only temp artifacts inside allowed scratch/state roots may be cleaned.
- **Validation rung:** `media-policy-release-real-media`

### Local output

- **State id:** `local_output`
- **Implemented:** yes
- **Owner:** Pipeline publish preparation
- **Allowed operations:** `verify size/hash evidence`, `write sidecar`, `copy to final partial`, `park pending publish`
- **Forbidden operations:** `delete before publish evidence`, `overwrite final output without transactional guard`
- **Artifacts:** `local output`, `pipeline sidecar`, `publish transaction id`
- **Recovery rule:** Local output can be retried, parked, or reconciled with manifest evidence.
- **Cleanup rule:** Delete only after validated publish or pending-publish cleanup rules run.
- **Validation rung:** `settings-pending-publish-diagnostics`

### Pending publish

- **State id:** `pending_publish`
- **Implemented:** yes
- **Owner:** engine publish pending-transaction and drain modules
- **Allowed operations:** `write pending manifest`, `move local output into pending store`, `drain with manifest evidence`
- **Forbidden operations:** `bypass manifest`, `drain from source media`, `discard parked output before validated publish`
- **Artifacts:** `*.manifest.json`, `parked media`, `parked sidecars`, `drain summary`
- **Recovery rule:** pending_move manifests are repairable; parked manifests remain retryable until drained or operator-reviewed.
- **Cleanup rule:** Parked local artifacts are removed only after server copy validates or already-published evidence is confirmed.
- **Validation rung:** `settings-pending-publish-diagnostics`

### Final output

- **State id:** `final_output`
- **Implemented:** yes
- **Owner:** Publish completion and completed manifest modules
- **Allowed operations:** `write completed manifest entry`, `write output summary`, `preserve sidecar evidence`
- **Forbidden operations:** `replace without transactional partial/backup flow`, `delete on sidecar write failure`
- **Artifacts:** `completed manifest`, `pipeline sidecar`, `output summary`
- **Recovery rule:** Completed evidence can reconcile UI state and prevent duplicate processing.
- **Cleanup rule:** Final outputs are not cleanup targets for pipeline temp cleanup.
- **Validation rung:** `settings-pending-publish-diagnostics`

### Failure review

- **State id:** `failure_review`
- **Implemented:** yes
- **Owner:** Failure state and desktop failure cleanup services
- **Allowed operations:** `write failure marker`, `archive cleared marker`, `dry-run clear workspace`
- **Forbidden operations:** `clear outside failure roots`, `clear without manifest evidence`
- **Artifacts:** `failure marker JSON`, `failure reports`, `clear manifest`
- **Recovery rule:** Operator can inspect markers and reports before guarded cleanup.
- **Cleanup rule:** Failure cleanup is limited to allowed failure workspace roots and can run dry-run first.
- **Validation rung:** `settings-pending-publish-diagnostics`

### Ready/archive/quarantine/trash-pending

- **State id:** `ready_archive_quarantine_trash`
- **Implemented:** no
- **Owner:** Not currently implemented as dedicated folders
- **Allowed operations:** `document as absent`, `map future work explicitly before adding states`
- **Forbidden operations:** `assume these folders exist`, `cleanup into these folders without a contract`
- **Artifacts:** `n/a`
- **Recovery rule:** Use pending publish, completed manifests, and failure review until these states are explicitly implemented.
- **Cleanup rule:** No cleanup rule because the states are not implemented.
- **Validation rung:** `docs-and-contract-checks`

## Transitions

| From | To | Trigger | Guard | Evidence |
|---|---|---|---|---|
| `source_library` | `scratch_copy` | source selected for processing | source path allowed and stable enough for copy | source identity, size, mtime, scratch path |
| `scratch_copy` | `processing` | copy verification succeeds | scratch copy is complete and visible only as scratch input | copy telemetry and scratch metadata |
| `processing` | `local_output` | remux/encode/subtitle/audio work completes | output exists and required sidecar/review policy is satisfied | local output, sidecars, logs, route decision |
| `local_output` | `pending_publish` | final publish is unsafe, unavailable, or deferred | pending manifest written and validated before park move completes | pending manifest and parked media |
| `local_output` | `final_output` | immediate publish succeeds | transactional partial/backup publish validates final copy | completed manifest and pipeline sidecar |
| `pending_publish` | `final_output` | pending drain succeeds | manifest evidence validates parked local copy and server copy | drain summary, completed manifest, output summary |
| `processing` | `failure_review` | stage failure or review-required condition | failure reason is classified | failure marker, report, logs |

## Validation

- `python scripts/dev/generate_lifecycle_map.py --check` fails when this generated map drifts.
- Lifecycle contract tests validate unique state ids and transition endpoints.
- Runtime behavior changes must update `app/contracts/lifecycle.py` first, then regenerate this file.
