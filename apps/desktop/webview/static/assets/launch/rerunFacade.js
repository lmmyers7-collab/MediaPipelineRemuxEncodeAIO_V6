(function () {
  function createLaunchRerunFacade(deps = {}) {
    const {
      appendCommandResult,
      byId,
      collectRerunMinimumWorkerCount,
      collectRerunNetworkStartDryRunRequest,
      collectRerunNetworkStartRequest,
      collectRerunPreviewRequest,
      collectRerunScopeRequest,
      collectRerunStartRequest,
      launchCoordinatorState,
      nextLaunchCommandFrame,
      queueRerunRouteDispatcher,
      refreshAll,
      rejectLaunchCommandWhileBusy,
      renderAllLaunchPreflights,
      renderJsonDetail,
      renderLaunchCommandResult,
      renderLaunchPreflight,
      RERUN_EXECUTION_LABELS,
      RERUN_POLICY_CHOICES,
      rerunExecutionTargetLabel,
      rerunIsNetworkMode,
      rerunPreviewRouteName,
      rerunQueuePreflightLines,
      rerunStartRouteName,
      setLaunchCommandBusy,
      setLaunchCommandButtonState,
      setText,
      updateLaunchCommandButtonStates,
    } = deps;

    const rerunWorkflowState = {
      lastRerunPreviewPayload: null,
      lastRerunResultsPayload: null,
      lastRerunCommandResult: null,
      rerunPreviewRefreshTimer: null,
    };
    let rerunPresentation = {};
    let rerunOrchestration = {};
    const rerunEvidenceFactory = window.__launchRerunEvidenceModule || {};
    delete window.__launchRerunEvidenceModule;
    const rerunEvidence = rerunEvidenceFactory.createLaunchRerunEvidenceModule({
      applyRerunPreviewButtonState: (...args) => rerunPresentation.applyRerunPreviewButtonState(...args),
      byId: typeof byId === "function" ? byId : window.byId,
      collectRerunScopeRequest,
      collectRerunStartRequest,
      launchCoordinatorState,
      renderAllLaunchPreflights: (...args) => renderAllLaunchPreflights(...args),
      RERUN_EXECUTION_LABELS,
      RERUN_POLICY_CHOICES,
      rerunExecutionTargetLabel,
      rerunPreviewBlockedReason: (...args) => rerunPresentation.rerunPreviewBlockedReason(...args),
      rerunStartRouteName,
      scheduleRerunPreviewRefresh: (...args) => rerunPresentation.scheduleRerunPreviewRefresh(...args),
      setText: typeof setText === "function" ? setText : window.setText,
      state: rerunWorkflowState,
      updateLaunchCommandButtonStates: (...args) => updateLaunchCommandButtonStates(...args),
    });
    const {
      rerunPreviewCounts,
      rerunPreviewScope,
      rerunCount,
      rerunLabelFromChoice,
      rerunPreviewConflictLines,
      rerunSummaryLines,
      clearElement,
      appendCell,
      appendRerunText,
      rerunStatusSeverity,
      setRerunReviewCount,
      rerunCurrentPhase,
      rerunReviewNextAction,
      renderRerunReviewHeader,
      rerunHistoryEntries,
      rerunHistoryRequestData,
      rerunHistoryData,
      rerunLatestStartHistoryEntry,
      rerunResultRequestData,
      rerunResultData,
      rerunEvidencePid,
      rerunSafeNextAction,
      renderRerunLifecycleEvidence,
      renderRerunRecentCsvs,
      rerunPolicyChoice,
      rerunExecutionLabel,
      setRerunHandlingText,
      renderRerunHandlingCard,
      rerunFinalReplacementSelected,
      rerunPolicySelectId,
      rerunPolicyRequestKey,
      rerunPolicyKindFromElement,
      rerunRawPolicySelection,
      rerunPolicyConflictReason,
      setRerunPolicySelectAuto,
      updateRerunPolicyOptionStates,
      applyRerunPolicySelectionRules,
      rerunPolicyChoiceForSummary,
      rerunSourceHandlingLine,
      renderRerunHandlingSummary,
      rerunStartPolicySummary,
      selectedOptionValues,
      renderRerunSelectOptions,
      renderRerunFilterOptions,
    } = rerunEvidence;

    const rerunPresentationFactory = window.__launchRerunPresentationModule || {};
    delete window.__launchRerunPresentationModule;
    rerunPresentation = rerunPresentationFactory.createLaunchRerunPresentationModule({
      appendCell,
      appendCommandResult: typeof appendCommandResult === "function" ? appendCommandResult : window.appendCommandResult,
      appendRerunText,
      byId: typeof byId === "function" ? byId : window.byId,
      clearElement,
      collectRerunPreviewRequest,
      collectRerunStartRequest,
      launchCoordinatorState,
      nextLaunchCommandFrame,
      queueRerunRouteDispatcher,
      refreshAll: typeof refreshAll === "function" ? refreshAll : window.refreshAll,
      rejectLaunchCommandWhileBusy,
      renderJsonDetail,
      renderLaunchCommandResult,
      renderLaunchPreflight,
      renderRerunLifecycleEvidence,
      renderRerunPreview: (...args) => rerunOrchestration.renderRerunPreview(...args),
      renderRerunReviewHeader,
      rerunExecutionLabel,
      rerunExecutionTargetLabel,
      rerunHistoryData,
      rerunHistoryEntries,
      rerunHistoryRequestData,
      rerunIsNetworkMode,
      rerunPolicyChoice,
      rerunPreviewConflictLines,
      rerunPreviewCounts,
      rerunPreviewRouteName,
      rerunQueuePreflightLines,
      rerunSourceHandlingLine,
      rerunStatusSeverity,
      setLaunchCommandBusy,
      setText: typeof setText === "function" ? setText : window.setText,
      state: rerunWorkflowState,
    });
    const {
      renderRerunPreviewTiles,
      rerunPreviewRowNumber,
      rerunPreviewRowText,
      pushRerunCategory,
      rerunPreviewCategoryTokens,
      renderRerunPreviewCategoryChips,
      renderRerunPreviewIdentity,
      renderRerunPreviewStatus,
      renderRerunPreviewReason,
      renderRerunPreviewDestination,
      renderRerunPreviewRows,
      renderRerunHistorySummary,
      renderRerunPolicyPanel,
      selectedRerunCsvCandidate,
      applyRerunOpenButtonState,
      inspectSelectedRerunCsv,
      openSelectedRerunCsv,
      rerunResultCountsLine,
      rerunManifestTitle,
      renderRerunResults,
      refreshRerunResults,
      requestRerunContinue,
      rerunPreviewBlockedReason,
      applyRerunPreviewButtonState,
      refreshRerunPreview,
      scheduleRerunPreviewRefresh,
      rerunCsvLeaf,
      renderRerunTopbarPending,
      renderRerunTopbarFinished,
    } = rerunPresentation;

    const rerunOrchestrationFactory = window.__launchRerunOrchestrationModule || {};
    delete window.__launchRerunOrchestrationModule;
    rerunOrchestration = rerunOrchestrationFactory.createLaunchRerunOrchestrationModule({
      appendCommandResult: typeof appendCommandResult === "function" ? appendCommandResult : window.appendCommandResult,
      applyRerunOpenButtonState,
      applyRerunPreviewButtonState,
      byId: typeof byId === "function" ? byId : window.byId,
      collectRerunMinimumWorkerCount,
      collectRerunNetworkStartDryRunRequest,
      collectRerunNetworkStartRequest,
      collectRerunStartRequest,
      nextLaunchCommandFrame,
      queueRerunRouteDispatcher,
      refreshAll: typeof refreshAll === "function" ? refreshAll : window.refreshAll,
      refreshRerunPreview,
      rejectLaunchCommandWhileBusy,
      renderJsonDetail,
      renderLaunchCommandResult,
      renderLaunchPreflight,
      renderRerunFilterOptions,
      renderRerunHandlingSummary,
      renderRerunHistorySummary,
      renderRerunLifecycleEvidence,
      renderRerunPolicyPanel,
      renderRerunPreviewRows,
      renderRerunPreviewTiles,
      renderRerunRecentCsvs,
      renderRerunReviewHeader,
      renderRerunTopbarFinished,
      renderRerunTopbarPending,
      rerunCsvLeaf,
      rerunIsNetworkMode,
      rerunQueuePreflightLines,
      rerunStartPolicySummary,
      rerunSummaryLines,
      setLaunchCommandBusy,
      setLaunchCommandButtonState,
      setText: typeof setText === "function" ? setText : window.setText,
      state: rerunWorkflowState,
      updateLaunchCommandButtonStates,
    });
    const {
      renderRerunPreview,
      networkDryRunFingerprint,
      networkDryRunSafeToApply,
      checkNetworkRerunStartDryRunFromForm,
      startNetworkRerunFromForm,
      startRerunFromForm,
    } = rerunOrchestration;


    return {
      ...rerunEvidence,
      ...rerunPresentation,
      ...rerunOrchestration,
    };
  }

  window.__launchRerunFacade = { createLaunchRerunFacade };
})();
