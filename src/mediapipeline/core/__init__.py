"""MediaPipelineRemuxEncodeAIO orchestrator target layout.

This package is the in-progress home for the Python orchestrator. Until
the Phase 3-6 migration completes, the live code still lives in
`src/mediapipeline/desktop/`. New contracts and scaffolding
land here first; callers migrate per domain in subsequent phases.

See docs/architecture/ARCHITECTURAL_OVERHAUL_PLAN.md and docs/adr/0001-folder-by-domain-layout.md.
"""
