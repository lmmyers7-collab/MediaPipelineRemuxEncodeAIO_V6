name: Audit SARIF

on:
  push:
    branches:
      - main
      - master
      - "codex/**"
      - "refactor/**"
    paths:
      - ".github/workflows/audit-sarif.yml"
  schedule:
    - cron: "17 7 * * *"
  workflow_dispatch:

permissions:
  actions: read
  contents: read
  security-events: write

jobs:
  ruff:
    name: Ruff SARIF
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Ruff
        run: python -m pip install "ruff>=0.8,<1"

      - name: Run Ruff
        continue-on-error: true
        run: ruff check --output-format=sarif --output-file=ruff.sarif .

      - name: Summarize Ruff SARIF
        if: always() && hashFiles('ruff.sarif') != ''
        run: |
          python - <<'PY' >> "$GITHUB_STEP_SUMMARY"
          import json
          from pathlib import Path

          data = json.loads(Path("ruff.sarif").read_text(encoding="utf-8"))
          count = sum(len(run.get("results", [])) for run in data.get("runs", []))
          print("### Ruff SARIF")
          print(f"Findings: {count}")
          PY

      - name: Upload Ruff SARIF artifact
        if: always() && hashFiles('ruff.sarif') != ''
        uses: actions/upload-artifact@v7
        with:
          name: ruff-sarif
          path: ruff.sarif
          if-no-files-found: error

      - name: Upload Ruff SARIF
        if: always() && hashFiles('ruff.sarif') != ''
        continue-on-error: true
        uses: github/codeql-action/upload-sarif@v4
        with:
          sarif_file: ruff.sarif
          category: ruff

  semgrep:
    name: Semgrep SARIF
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Semgrep
        run: python -m pip install "semgrep>=1,<2"

      - name: Run Semgrep
        continue-on-error: true
        run: semgrep scan --config p/default --sarif --output semgrep.sarif .

      - name: Summarize Semgrep SARIF
        if: always() && hashFiles('semgrep.sarif') != ''
        run: |
          python - <<'PY' >> "$GITHUB_STEP_SUMMARY"
          import json
          from pathlib import Path

          data = json.loads(Path("semgrep.sarif").read_text(encoding="utf-8"))
          count = sum(len(run.get("results", [])) for run in data.get("runs", []))
          print("### Semgrep SARIF")
          print(f"Findings: {count}")
          PY

      - name: Upload Semgrep SARIF artifact
        if: always() && hashFiles('semgrep.sarif') != ''
        uses: actions/upload-artifact@v7
        with:
          name: semgrep-sarif
          path: semgrep.sarif
          if-no-files-found: error

      - name: Upload Semgrep SARIF
        if: always() && hashFiles('semgrep.sarif') != ''
        continue-on-error: true
        uses: github/codeql-action/upload-sarif@v4
        with:
          sarif_file: semgrep.sarif
          category: semgrep
