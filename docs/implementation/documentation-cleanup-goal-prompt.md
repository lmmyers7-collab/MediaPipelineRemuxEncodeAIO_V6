# Documentation Cleanup Goal Prompt

Goal: complete the four-phase documentation cleanup from the 2026-06-24 repo scan without changing runtime behavior, media policy, command routes, WebView authority, or source/scratch/output movement.

Work from the repository root and follow `AGENTS.md`. Treat `docs/CURRENT_PROJECT_STATE.md`, `docs/OPEN_WORK_CHECKLIST.md`, `docs/architecture/DECISIONS_AND_HISTORY.md`, source/tests, and generated maps as the evidence hierarchy. Create one low-risk docs change packet under `ops/release/changes/unreleased/`, record every touched path, and do not commit.

Phase 1: fix canonical drift. Replace broken `docs/adr/` references in root `README.md` and `docs/architecture/ARCHITECTURE.md` with the current decision authority, `docs/architecture/DECISIONS_AND_HISTORY.md`, unless a real `docs/adr/` index is intentionally restored. Reconcile the stale Network decision in `DECISIONS_AND_HISTORY.md`: WebView Network is not read-only-only anymore; it may call backend-owned provider-guarded Local API lifecycle/setup routes, while lifecycle, queue, claim, done-report, settings-save, publish, rename, and media mutation authority remain backend-owned. Refresh `docs/DOCS_INDEX.md` and `docs/ARCHIVED_MD_INDEX.md` so their dates, active folders, archive notes, `DesktopApp/docs/`, and `docs/RealMediaValidationRuns/` entries match the current tree.

Phase 2: eliminate stale active implementation plans. Archive or relabel completed plans that contradict current status, starting with `docs/implementation/watch-folder-autostart/PLAN.md`, `docs/implementation/objective-quality-verification-plan.md`, and `docs/implementation/dynamic-hdr-preservation/PLAN.md`. Move completed plan bodies under `docs/archive/docs-housekeeping/2026-06-24-doc-prune/implementation/` when their useful current status is already captured in canonical docs. Keep `docs/implementation/encoder-breadth-av1-plan.md` active only if it still matches the current open encoder-breadth workstream.

Phase 3: triage evidence snapshots. Treat `docs/ai-audits/` and `docs/reviews/` as evidence snapshots, not active guidance. Keep only review files directly referenced by current docs, such as active findings/disposition ledgers. Archive completed review packs once their decisions or outcomes are summarized in canonical docs. Update `DOCS_INDEX.md` so readers know which audit/review files are current references and which are historical evidence.

Phase 4: refresh generated navigation. After all doc moves/edits, regenerate or check generated docs using the bundled Python runtime: `refresh_summaries --changed`, `generate_project_index`, and `generate_feature_file_map`. Prune orphan summaries only through the supported summary tooling. Then run `check_active_doc_references`, `refresh_summaries --check`, `generate_project_index --check`, and `generate_feature_file_map --check`. Record validation results in the change packet and call out unrelated dirty files separately.

Acceptance criteria: no active doc claims completed work is unimplemented; no active link points at missing `docs/adr/`; Network authority wording is consistent across architecture/current-state docs; generated navigation checks pass; stale evidence is archived or explicitly labeled; the change packet validates or documents any unrelated uncovered dirty files.
