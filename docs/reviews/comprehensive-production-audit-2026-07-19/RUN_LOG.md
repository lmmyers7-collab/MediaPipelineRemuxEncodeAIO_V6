# Run Log

## 2026-07-19 — Campaign initialization

- Read the supplied persistent-goal objective and created the active audit goal.
- Read `AGENTS.md`, the canonical navigation set, the no-touch register, and the
  validation ladder before high-risk review.
- Applied the local `production-audit` skill: local evidence only, explicit
  missing evidence, no unapproved remote scanners or data upload.
- Observed a substantial pre-existing dirty overlay and did not reset, stash,
  discard, or absorb it.
- Confirmed existing packet `MP-CHANGE-2026-0719-001` is unrelated rename work.
- Created audit packet `MP-CHANGE-2026-0719-002` and this dated review pack.
- Started bounded read-only streams for provenance, historical reconciliation,
  and architecture/coverage inventory.
- Invoked no live mutation route and no representative real-media processing.

### Phase status

- Active phase: baseline capture, historical reconciliation, coverage setup.
- Verified new findings: none at initialization.
- Blockers: none; representative real-media remains intentionally gated.
- Next: normalize baseline evidence, merge historical IDs, and source-verify
  inventory drift before assigning new finding IDs.

## 2026-07-19 — Baseline and historical reconciliation completed

- Captured exact HEAD `a8bf6e1dda9629e6f818ba12f2c8274362923372`, branch,
  upstream, recent history, local toolchain, initial 626-file tracked overlay,
  47 untracked paths, zero staged paths, and validation claims/limits.
- Recorded a concurrent user delta separately after two rename-domain files
  changed during capture; audit provenance was not reassigned.
- Reconciled all requested historical packs. Retained original identifiers and
  classified documentary closures, evidence gaps, mixed/accepted CodeQL rows,
  audit-process incidents, and unresolved runtime/real-media requirements.
- Independently verified two current P2 carry-forwards against generated
  summaries, unchanged HEAD source, tests, and focused reproductions.
- `CSW-2026-07-09-SCHEDULE-001`: isolated restart inside the debounce window
  reproduced silent candidate loss.
- `CSW-2026-07-09-MAINTENANCE-002`: focused PowerShell dry-run check passed and
  proved the external checkpoint is intentional; the defect is contradictory
  UI contract language, not manifest mutation.
- Promoted both records to `FINDINGS.md` and `FINDINGS.json`. No new IDs were
  created and no remediation was started.
- Added foundation, historical, ownership, workflow, independent-pass,
  cross-cutting failure, and campaign-gate rows to the coverage matrix.
- Recorded nine inventory/authority drift observations for source/generator
  verification. They remain candidates, not findings.

### Severity count

- P0: 0 open.
- P1: 0 open from current reconciliation.
- P2: 2 verified open carry-forwards.
- P3: 0 verified open.

### Next

- Run safe artifact/schema/change-packet checks.
- Source-verify inventory drift, beginning with browser smoke counts.
- Begin vertical workflow review with startup/process lifecycle and maintain
  baseline applicability for every conclusion.

## 2026-07-19 — Vertical review tranche: validation, media, and lifecycle

- Kept discovery/remediation separation: only audit artifacts and the audit
  packet were changed. No source, production runtime state, live API mutation,
  force-close, publish, rename, settings save, queue start, or real-media run
  was performed.
- Verified `CPA-2026-07-19-001` (P3): the canonical smoke-wrapper check remains
  green while active prose counts and direct-only module coverage are stale.
- Verified overlay-only `CPA-2026-07-19-002` (P2): the HDR10 metadata helper
  drops verification heartbeat arguments; a no-write mock captured null/zero.
- Independently verified `CPA-2026-07-19-003` (P1): same-path/size/mtime source
  replacement reuses unequal stale scratch bytes.
- Independently verified `CPA-2026-07-19-004` (P1): abnormal shell exit releases
  the only singleton mutex while the documented backend survives; relaunch
  unconditionally starts another backend/watcher.
- Independently verified `CPA-2026-07-19-005` (P1): both forced-cleanup methods
  can throw while the API still returns `ok=true` and schedules backend exit.
- Verified `CPA-2026-07-19-006` (P2): Tauri maps `Child::try_wait` error to
  successful exit and releases process ownership, contrary to the official
  Rust process contract.
- Revalidated historical `CSW-2026-07-09-HOME-003` (P2): an actual-module Node
  DOM-stub run showed a malformed string boolean rendering Safe/ready facts
  beside a correctly blocked command state.
- Revalidated historical `CSW-2026-07-09-TELEMETRY-004` (P2): an actual-module
  Node run rewrote the first backend-stale warning to active/fresh.
- Verified `CPA-2026-07-19-007` (P2): nine persistent OCR prerequisite codes
  are retryable and whole subtitle families are registered transient despite
  operator actions requiring configuration/tool repair.
- Recorded each reproduction and evidence limit in the validation ledger.
- The worktree remained concurrently active; the latest snapshot had 696
  porcelain paths. This does not replace the initial provenance snapshot.

### Severity count after tranche

- P0: 0 open.
- P1: 3 verified open, all independently verified.
- P2: 7 verified open (four current-campaign IDs and three historical IDs).
- P3: 1 verified open.

### Next

- Complete queue/settings and publish/rename/network vertical traces.
- Reconcile remaining historical source-verification candidates.
- Expand cross-cutting failure-mode and independent-pass evidence before
  selecting broader safe validation rungs.

## 2026-07-19 — Vertical review tranche: settings authority and concurrency

- Reclassified historical `CSW-2026-07-09-SETTINGS-001` as still present (P1):
  production preview accepts a real hidden auth-token value across the browser
  request boundary and only redacts the later response.
- Reclassified historical `CSW-2026-07-09-SETTINGS-002` as still present (P1):
  Python projects arbitrary legacy extras and the actual PowerShell runtime
  merge materializes them as script variables, disproving the inert claim.
- Reclassified historical `CSW-2026-07-09-SETTINGS-003` as partially fixed but
  still present (P1): the sequential stale guard works, while a deterministic
  separate-lock race produced two successes and one lost disjoint update.
- Independently verified all three P1s using focused test or isolated no-write
  reproduction; no production settings file or live save route was touched.
- Verified `CPA-2026-07-19-008` (P3): actual settings counts are
  206/156/146/10/60 while two active inventories disagree and no enforcing
  freshness check was found.
- Updated workflow, independent-pass, authority, failure-mode, disposition, and
  campaign-gate records. Discovery/remediation separation remains intact.

### Severity count after settings tranche

- P0: 0 open.
- P1: 6 verified open, all independently verified.
- P2: 7 verified open.
- P3: 2 verified open.

### Next

- Complete queue launch and override/retry tracing, beginning with focused
  verification of historical malformed-manifest fail-closed behavior.
- Continue publish/rename/network and remaining cross-cutting passes.

## 2026-07-19 — Queue historical verification checkpoint

- Verified historical `QUEUE-001` fixed on the current overlay: the formerly
  NUL-filled rerun policy compiles and imports; committed HEAD had not contained
  the corrupt bytes.
- Verified historical `QUEUE-002` fixed on current HEAD and overlay: focused
  Python recovery/write coverage and the PowerShell processing-consumer check
  both reject a malformed persisted override.
- Verified historical `QUEUE-003` fixed on current HEAD and overlay: canonical
  contract, both active route inventories, and a focused contract test include
  manual-order `position`.
- Verified historical `QUEUE-004` fixed on the current overlay: three affected
  source/summary pairs pass currentness and orphan checks.
- Two initial unit-test selectors used incorrect class names and failed at
  loader discovery before running tests; corrected selectors passed and both
  attempts are retained in the validation ledger.
- No queue launch, priority write, override write, rerun, or media processing
  was invoked. WF-03 remains in progress for the broad current overlay and
  duplicate/recovery paths.

## 2026-07-20 — Queue current-overlay validation tranche

- Ran 177 isolated Python queue and launch tests across 11 modules; all passed
  in 56.4 seconds.
- The first combined PowerShell batch timed out at 124 seconds without a
  per-test result and left two audit-owned PowerShell processes. Their exact
  PIDs/command lines were verified and only those two processes were stopped.
  No production state, route, media, or external share was involved.
- Re-ran the two small worker suites directly; slot and claim-lifecycle checks
  both passed in under three seconds each.
- Checkpointed the large queue-engine harness in memory with bounded child-tree
  termination. The slow case is the deliberate 505-row uncapped accepted-run
  snapshot test, not a deadlock. A 10-row variant took about five seconds and a
  50-row variant about 30 seconds.
- Re-ran the exact unmodified queue-engine harness with an adequate ceiling; it
  passed in 269.5 seconds. The runtime remains a limitation for test ergonomics
  and production queue-preview latency assessment, not a promoted finding
  without operator-latency evidence.
- WF-03 remains in progress: isolated contracts and synthetic PowerShell engine
  behavior are strong, while real process/media, browser, restart, and field
  latency evidence remain gated or pending.

## 2026-07-20 — Pending Publish boundary tranche

- Re-read the no-touch register before reviewing the high-risk publish/drain
  boundary. No live drain, repair, reconcile, recovery, or production-state
  operation was invoked.
- Verified historical `PENDING-001` fixed: WebView evidence is advisory,
  submission remains enabled, and the browser smoke sends the unchanged
  `drain_pending_pushes` intent to backend authority.
- Verified historical `PENDING-002` fixed: `Refresh-PendingPublishIndex` is
  read-only; explicit recovery owns file/manifest changes and intent/result
  evidence. Ownership and safety fixture suites passed.
- Reclassified historical `PENDING-003` as still present (P3), disproving the
  prior superseded/fixed entry. Ordinary orphan discovery emits no proposal,
  the dry-run only consumes already-attached proposal fields, successful tests
  inject the field, and the WebView still offers a dry-run that cannot succeed
  from normal product state.
- Focused ordinary-orphan tests passed and prove the fail-closed dead end. This
  is an operability/UX finding, not a request to relax the backend proposal
  invariant.
- Pending ownership/safety PowerShell checks and the browser-backed advisory
  drain smoke passed. Representative deferred-publish/interrupted-drain/final
  placement evidence remains explicitly gated.

### Severity count after Pending Publish tranche

- P0: 0 open.
- P1: 6 verified open, all independently verified.
- P2: 7 verified open.
- P3: 3 verified open.

## 2026-07-20 — Rename safety and history tranche

- Reconciled all five historical rename findings against current `HEAD`, the
  overlay, implementation source, inventories, and focused isolated tests.
- Verified `RENAME-001` fixed: derived media and sidecar destinations are
  checked against the configured root authorizing their source, and an escape
  requires explicit outside-root confirmation.
- Verified `RENAME-002` fixed: undo preflight pairs metadata backups to recorded
  operations and boundary-checks the backup and restore targets before any undo
  mutation.
- Verified `RENAME-003` fixed: apply rejects a missing or stale preview
  fingerprint after rebuilding the current backend plan.
- Verified `RENAME-005` fixed: the active inventory now accurately names the
  facade and Local API active-work tests, which assert no plan/apply call and no
  filesystem mutation.
- Reproduced `RENAME-004` with the actual frontend modules and promoted it as a
  P2 under its original ID. After successful undo, history refresh replays the
  latest apply result, clears the completion flag, and enables the same
  manifest again. Backend manifest authority prevents a duplicate reversal,
  but the operator state is false and the second action is futile.
- Ran six focused rename modules with bundled Python: 57 tests passed in 13.4
  seconds. All mutations used temporary fixtures; no live rename preview,
  apply, undo, dialog, media, or LocalBase state was invoked.
- WF-10 remains in progress for packaged refresh/restart, representative
  interruption/rollback, and broader current-overlay behavior.

### Severity count after rename tranche

- P0: 0 open.
- P1: 6 verified open, all independently verified.
- P2: 8 verified open.
- P3: 3 verified open.
