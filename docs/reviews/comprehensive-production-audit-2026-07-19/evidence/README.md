# Evidence Directory

This directory stores compact, reviewable audit evidence. Large raw outputs are retained only when appropriate for repository storage; otherwise the validation ledger records exact commands, summaries, hashes, locations, and limitations.

Evidence must not contain secrets, personal configuration, media content, private topology, or live production mutation output.
