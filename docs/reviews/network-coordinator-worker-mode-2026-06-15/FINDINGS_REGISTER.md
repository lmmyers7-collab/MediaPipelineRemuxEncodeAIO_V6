# Network Coordinator/Worker Mode Findings Register

Date: 2026-06-15

This register deduplicates the 14 worker reports. Severity follows the highest justified worker severity after master validation. Worker IDs list every worker that reported or materially supported the same issue.

## Counts

| Severity | Count |
|---|---:|
| P1 | 14 |
| P2 | 19 |
| P3 | 2 |
| Total | 35 |

## Confirmed Findings

| ID | Severity | Risk area | Workers | File / line / symbol | Impact | Evidence | Fix direction | Validation |
|---|---|---|---|---|---|---|---|---|
| NCW-P1-01 | P1 | auth/secret | W01 | `src/mediapipeline/desktop/network/coordinator_auth.py:13-17`, `_load_or_generate_token` | A too-short configured coordinator token can start a LAN coordinator with a weak HMAC secret. | Startup returns any nonblank `CoordinatorAuthToken`; live `update_auth_token` rejects tokens shorter than 16 chars at lines 91-101. | Use one shared token-strength validator for config load and live rotation. Fail closed on invalid configured tokens and keep logs redacted. | Add tests for short configured token rejection, strong configured token acceptance, blank fallback, and no raw token in logs. |
| NCW-P1-02 | P1 | auth/secret diagnostics | W01,W02,W07 | `src/mediapipeline/desktop/network/cluster_log.py:19-30`, `coordinator_http_handlers.py:533-542`, `format_cluster_log_line` | Worker-controlled token-like values can be written to `cluster.log` through worker name, role, event, job/source fields, or timestamp. | Only `entry.message` is passed through `redact_network_secret_text`; other fields are only control-character sanitized. `/api/log` also redacts only message. | Redact every rendered cluster-log field after display sanitization, including worker id/name, role, level, event, job id, source basename/path, and worker timestamp. | Add formatter and `/api/log` tests with token-like values and credential URLs in every field. |
| NCW-P1-03 | P1 | claim/state ownership | W02,W03 | `src/mediapipeline/desktop/network/coordinator_http_handlers.py:270-291`, `registry.py:400-405`, `registry.py:503-504`, `_http_done`, `complete`, `unclaim` | Any authenticated client that knows a job id can omit `worker_id` and complete or release another worker's job. | HTTP `/api/done` explicitly allows empty `worker_id`; registry owner checks only reject non-empty mismatches. | Require non-empty valid `worker_id` on all external done/release reports. Move ownerless transitions to an internal-only helper if still needed. | Add endpoint tests for omitted/empty worker id across success, failure, terminal failure, and release; assert registry/queue unchanged. |
| NCW-P1-04 | P1 | claim/state durability | W02 | `src/mediapipeline/desktop/network/coordinator_http_handlers.py:201-259`, `_http_claim` | A claim can be durably saved but never delivered to the worker if response serialization or socket write fails, leaving work in-flight until stale reclaim. | The handler saves registry at lines 201-202, builds `ClaimResponse` at 248-258, and sends at 259 without rollback for serialization/write failure. | Pre-serialize the exact response before durable save, or catch structured send failure and rollback/persist the rollback before returning. | Add tests for non-strict response payload and write failure after save; assert source is no longer in-flight. |
| NCW-P1-05 | P1 | source/claim identity | W03 | `src/mediapipeline/desktop/network/registry.py:271-302`, `is_in_flight`, `_claimed_paths` | Case/slash/UNC spelling variants of the same Windows source can be claimed as separate jobs, risking duplicate processing and output/publish races. | `claim()` keys `_claimed_paths` by raw `source_path`; `is_in_flight`, recent completions, queue removal, and prior-failure matching use raw or only partial normalization. | Introduce one Windows/UNC source identity normalizer for claim keys, completion grace, failure ledger, prior failures, and queue removal while preserving original display path. | Add claim/load/retry/removal tests proving `C:\Media\Movie.mkv` and `c:/media/movie.mkv` cannot both process. |
| NCW-P1-06 | P1 | done/state durability | W04 | `src/mediapipeline/desktop/network/use_cases/done_outcome.py:56-64`, `:79-88`, `:199-208`, `:226-243`; `coordinator_http_handlers.py:325-341`, `:414-431` | Workers can clear their only pending terminal report after the coordinator has not durably saved the terminal state. Queue removal can also happen before durable acceptance. | Done outcome mutates memory and schedules/removes queue records before `_save_registry_after_done`; save failures are logged but not raised. Release save failures also still return OK. | Make done/release acceptance contingent on durable registry save. Keep queue removal behind a successful durable transition; return retryable failure on save error. | Add save-failure tests for complete and unclaim; assert HTTP result is not accepted, pending worker report remains, and queue record is not removed. |
| NCW-P1-07 | P1 | done/evidence durability | W04 | `src/mediapipeline/desktop/network/worker_state.py:202-215`, `_flush_pending_done_report`; `coordinator_http_handlers.py:296-312`, `:377-393` | Late terminal reports after reclaim can be discarded without coordinator-owned durable evidence of success, pending publish, terminal failure, route, or reason. | Worker treats coordinator 404 as unrecoverable and clears state; coordinator 404 does not record the terminal payload. | Add a coordinator late-terminal-report ledger and return a distinct durable `late_recorded` status; only clear worker pending reports after `ok` or `late_recorded`. | Add stale-reclaim plus late success/pending-publish tests proving evidence is recorded without closing a new active claim. |
| NCW-P1-08 | P1 | worker process/claim state | W05 | `src/mediapipeline/desktop/application/network_lifecycle_provider.py:126-175`; `worker_claims.py:142-176`, `_watch_claimed_process`, `_start_claimed_encode` | If watcher thread start fails after process launch, the worker clean-releases the claim while the launched process may continue without terminal reporting. | `_watch_claimed_process` stores process then starts a thread without a guard; propagated exception is caught as encode-start failure and `_do_release` is called. | Treat watcher startup failure as post-launch failure: terminate/kill the process, clear active state, and report failed done or preserve pending failure evidence. Do not clean-release while process can run. | Add a test patching watcher `Thread.start` to raise after fake process launch; assert process cleanup and no clean release. |
| NCW-P1-09 | P1 | worker source handoff | W06 | `src/mediapipeline/desktop/network/protocol.py:149-153`; `worker_parts/tasks.py:12-14`, `:44-60`; `worker_record.py:12-15`; `network_lifecycle_provider.py:560-564` | A malformed `ok` claim missing identity can reach backend launch as `single_file='.'`, violating coordinator-assigned-source boundaries. | `ClaimResponse.from_dict` defaults missing `job_id`/`source_path` to empty; `make_queue_record` converts `Path("")` to `Path('.')`; provider rejects only empty string, not `"."`. | Reject `ok` claims unless job id and source path are non-empty and source policy valid before building a queue record. Release only trustworthy malformed identities. | Add worker poll-loop tests for missing/blank identity; assert no backend launch and bounded release/log behavior. |
| NCW-P1-10 | P1 | worker crash recovery | W06 | `src/mediapipeline/desktop/network/worker_state.py:146-153`, `:196-198`; `worker.py:169-172`; `worker_loops.py:108-116` | A worker restart with coordinator offline can leave active crash state unresolved while polling starts and overwrites the single-slot state with new work. | `_crash_recover` logs and returns on POST failure; constructor still starts polling; `_flush_pending_done_report` returns true for readable active-only state with no pending report. | Treat unresolved active-only state as claim-blocking until crash recovery is durably accepted, deliberately discarded, or operator-repaired. | Add startup/poll-loop test with active `worker_state.json` and failed crash report; assert `/api/claim` is not called. |
| NCW-P1-11 | P1 | auth/secret journal | W07 | `src/mediapipeline/desktop/api/handler.py:100-109`; `command_journal_policy.py:18-26`, `:80-82` | A malformed `join-cluster` request can persist the secret-bearing `join_blob` into command history before route-level unjournaled suppression runs. | Validation failure records `request=body` immediately; sensitive key terms include token/secret/password/auth but not `join_blob`; successful route suppression occurs only after handler response data. | For validation failures, consult route metadata and skip request journaling for `journaled=false` or `effect=secret-transfer` routes. Add `join_blob` to sensitive keys. | Post a real join blob plus unknown field to `/api/network/worker/join-cluster`; assert `/api/commands` has no blob/token. |
| NCW-P1-12 | P1 | lifecycle/active work | W08 | `src/mediapipeline/desktop/application/network_lifecycle_provider.py:377-405`, `:433-456`; `contract_command.py:810-823` | Ordinary confirmed Network stop can abort active worker or coordinator-local pipeline work even though the contract says abort/partial cleanup is separate. | Stop paths call `app.abort_current_worker_job(...)`; contract says worker stop preserves pending done reports/state and abort cleanup remains separate. | Make stop cooperative: stop polling/new claims, report active work, and wait or return blocked/review instead of killing. Put forced abort behind a separate explicit command. | Add tests with active worker/coordinator-local jobs asserting stop does not call `kill_process_tree`/`terminate` and preserves claims/state. |
| NCW-P1-13 | P1 | publish/output evidence | W13 | `src/mediapipeline/desktop/application/network_lifecycle_provider.py:149-163`, `:560-593`; `ops/pipeline/engine/process/worker_result.ps1:60-81` | Network workers report exit-code-only completion and drop backend route, publish state, output path/size, reason, and PowerShell claim/run validation evidence. | Provider watcher calls `dispatcher.mark_done` with generic status/route and no result artifact; PowerShell worker-child result has richer `local_worker_result.v1` fields. | Launch claimed network work with a worker-result artifact path or equivalent process-result bridge, validate claim/run identity, and map result fields into `DoneRequest`. | Add provider tests for success, pending publish, failure, wrong claim/run, missing/unreadable result, and done field propagation. |
| NCW-P1-14 | P1 | test coverage | W12 | `tests/python/desktop/test_network_workflow.py:74`, `:108`, `:886`; missing concurrent `/api/claim` test | Duplicate processing is one of the highest-risk distributed failures, but the suite lacks a concurrent two-worker claim race regression. | Existing tests cover duplicate loaded registry rows and single-threaded claim behavior, not simultaneous `/api/claim` calls against one source. | Add a race-focused test using two worker ids, one queue record, and a barrier or real threaded handler calls. | Require exactly one OK claim, one empty/retry response, and one persisted in-flight job/source. |
| NCW-P2-01 | P2 | diagnostics/evidence | W02 | `src/mediapipeline/desktop/network/use_cases/done_outcome.py:152-198`; `coordinator_state.py:82-93`, `:116-126` | Failure and quarantine events can be dropped because unsupported kwargs raise `TypeError` and `_safe_log_cluster_event` swallows it. | `done_outcome` passes `reason_code`, `reason`, `consecutive_count`, and `max_job_retries`; `log_cluster_event` does not accept them. | Remove unsupported kwargs and fold metadata into redacted message, or extend log event schema safely. | Add production-signature tests for failed/quarantined jobs with real `log_cluster_event` append path. |
| NCW-P2-02 | P2 | claim/state invariant | W03 | `src/mediapipeline/desktop/network/registry.py:271-302`, `claim` | Runtime duplicate `job_id` overwrites the existing job and can leave stale `_claimed_paths` entries. | `claim()` rejects duplicate source path only, then assigns `self._jobs[job_id] = job`; load-time duplicate rejection exists but live mutation lacks it. | Validate non-empty job id/worker id/source path and reject `job_id in self._jobs` before mutating. | Add registry unit test proving duplicate job id returns false and leaves original state intact. |
| NCW-P2-03 | P2 | reaper evidence durability | W04 | `src/mediapipeline/desktop/network/registry.py:591-622`; `coordinator_lifecycle.py:116-159`; `coordinator_state.py:116-126` | If cluster logging fails, stale reclaim deletes the active job with no durable reclaim reason/source/job evidence. | `reclaim_stale()` removes job and only stores short recent-completion quarantine; `_safe_log_cluster_event` is best effort. | Persist a bounded reclaim ledger with job id, worker id/name, source, last heartbeat, reclaim time, and timeout. | Add stale-reaper test with log append failure; assert durable reclaim record survives save/load and late done can correlate. |
| NCW-P2-04 | P2 | worker release durability | W05,W06 | `src/mediapipeline/desktop/network/worker_claims.py:34-51`, `:53-74`; `worker_loops.py` pre-start release callers | Malformed/path-map/record-build release POST failures are logged but not persisted for retry, so coordinator depends on stale timeout. | Raw-identity release helper swallows `_http_post` exceptions; normal `ClaimedJob` release path saves pending reports, but wire-only paths do not. | Persist a minimal pending release report for trustworthy wire identities or block polling until release is accepted. | Add tests for release POST failure after malformed response, path-map failure, and record-build failure; assert retry before next claim. |
| NCW-P2-05 | P2 | protocol parsing | W07 | `src/mediapipeline/desktop/network/protocol.py:149-160`, `ClaimResponse.from_dict` | Malformed claim metadata can invert coordinator intent because `bool("false")` is true and integer coercion accepts loose values. | `priority=bool(...)`, `retry_on_failure=bool(...)`; `DoneRequest` already uses strict optional boolean helpers. | Reuse strict boolean coercion for claim booleans and reject bool/fractional values for protocol integers. | Extend `test_network_protocol_runtime.py` for string/number claim boolean rejection and integer edge cases. |
| NCW-P2-06 | P2 | lifecycle dry-run | W08 | `src/mediapipeline/core/network/lifecycle_facade.py:420-430`, `_network_lifecycle_dry_run_data` | Stop dry-runs can report safe while in-memory worker/coordinator-local work is active. | `active_work.reported_worker_count` is hard-coded to zero and stop preconditions are idempotent. | Inspect `_network_dispatcher_runtime`, active worker job/process state, and pending done state in dry-run payloads. | Add dry-run tests with active worker/coordinator-local jobs and assert non-clean active-work evidence. |
| NCW-P2-07 | P2 | worker lifecycle startup | W08 | `src/mediapipeline/desktop/application/network_lifecycle_provider.py:414-431`; `worker.py:171-172`; `_NetworkRuntimeApp._worker_start_single_file` | A fast claim can arrive before `app.dispatcher` and runtime state are attached, causing a false encode-start failure/release during worker startup. | `WorkerDispatcher(app)` starts polling in constructor; `app.dispatcher = dispatcher` happens after construction. | Make worker dispatcher startup two-phase: construct without polling, attach runtime/app dispatcher, then start polling. | Add a startup race test where immediate poll claim must see attached dispatcher and not release a valid claim. |
| NCW-P2-08 | P2 | Tauri route contract | W08,W14 | `apps/desktop/tauri/src-tauri/src/backend_contract/routes.rs:110-118`; `contract_command.py:723-764` | Tauri startup route checks can pass while missing current Network setup/discovery/join routes. | Rust required routes omit `POST /api/network/coordinator/join-blob`, `POST /api/network/worker/discover-coordinators`, and `POST /api/network/worker/join-cluster`; Python contract defines them. | Add the three routes to Rust required metadata and add semantic checks for effect/confirmation/frontend exposure where supported. | Rerun `tests.python.desktop.test_tauri_shell_scaffold`; keep route parity green. |
| NCW-P2-09 | P2 | path-map correctness | W09 | `src/mediapipeline/core/network/join.py:156-161`; `path_map.py:31-58` | Worker join import can reorder overlapping manual path-map prefixes and resolve claims to the wrong worker root. | `worker_join_patch_from_blob` serializes a dict with `sort_keys=True`; `apply_source_path_map` uses first-match ordered prefixes. | Preserve manual mapping order and sort only auto-derived mappings by descending prefix length if needed. | Add overlapping-prefix join import regression and assert `apply_source_path_map` still resolves to the deeper manual target. |
| NCW-P2-10 | P2 | source path safety | W09 | `src/mediapipeline/desktop/network/path_map.py:31-59`; `library_roots.py:29-36` | Manual/legacy path-map fallback can resolve outside intended worker source roots via relative replacement or `..` tail segments. | Library-relative paths reject unsafe values, but manual `parse_source_path_map` accepts any non-empty pair and returns `replacement + tail`. | Validate manual prefixes/replacements, reject relative/drive-relative/parent segments, and normalize final mapped path under replacement root. | Add direct path-map tests and worker-loop unstartable-release tests for unsafe mapped claims. |
| NCW-P2-11 | P2 | WebView operator evidence | W10 | `apps/desktop/webview/static/assets/settingsView.builders.network.js:420-443`; `contract_command.py:710-721` | The row Test can imply a specific path-map row is backend-accessible even though the backend route only checks generic saved config paths. | UI displays Sample/Resolved for the row, then calls `runNetworkWorkerTestConnection({render:false})`, whose contract accepts only `timeout_seconds`. | Reword UI as local rewrite preview plus generic backend preflight, or add a backend-owned no-mutation row-specific path-map validation route. | Add browser/static tests preventing row-specific accessibility wording without row-specific backend evidence. |
| NCW-P2-12 | P2 | WebView smoke coverage | W10 | `tests/webview/test_webview_browser_network_smoke.py:496-503`; `networkView.js` tab persistence; `app.js` UI preference sync | The assigned browser smoke currently fails before completing no-mutation checks because it treats expected UI preference sync as unexpected POST. | Allowlist omits `/api/ui-preferences`; failed run observed `storage.mediapipeline-network-tab`. | Allow only the exact UI preference payload, or disable shared UI preference sync inside the smoke. | Rerun `tests/webview/test_webview_browser_network_smoke.py` and `ops/scripts/smoke/Test-WebViewBrowserNetworkSmoke.ps1`. |
| NCW-P2-13 | P2 | read DTO drift | W11 | `src/mediapipeline/core/network/facade.py:656-701`; `worker.py` runtime descriptor | Auto-derived library path maps can be reported as saved-config drift, prompting unnecessary restart/reconfiguration. | Saved comparison uses only `WorkerSourcePathMap`, while running descriptor reports effective manual plus auto map. | Split runtime descriptor evidence into manual, auto, and effective maps; compare saved manual only against running manual. | Add drift test where saved map is blank and auto map active; expected status should be match/auto-map-active, not drift. |
| NCW-P2-14 | P2 | read DTO state evidence | W11 | `src/mediapipeline/core/network/facade.py:408-420`, `_safe_worker_state`; state file row builder | Malformed `worker_state.json` warns globally but still appears as a clean present state-file row. | `_safe_worker_state` returns `{}` and appends warning on read failure; state row is later built from file stat only. | Carry structured worker-state read errors into the state-file row and mark it `unreadable` with bounded redacted error. | Add test for malformed worker state asserting warning and `state_files.worker_state.status == "unreadable"`. |
| NCW-P2-15 | P2 | read DTO lifecycle evidence | W11 | `src/mediapipeline/core/network/facade.py:517-542`, `_runtime_status_from_lifecycle`; progress helpers | Stale active worker state can report `Runtime: Stopped (match)` while progress evidence says a local worker claim is active. | Runtime severity blocks only on pending done or unreadable rows, not active `job_id` in worker state when lifecycle memory says stopped. | Treat active worker state with stopped lifecycle as warning/blocked stale-claim evidence and add actionable repair guidance. | Add test with active `worker_state.json` plus stopped lifecycle; assert non-match severity and explicit warning. |
| NCW-P2-16 | P2 | docs/contract drift | W11,W12,W14 | `docs/architecture/NETWORK_LIFECYCLE_COMMAND_CONTRACT.md:53`; `docs/CURRENT_PROJECT_STATE.md:24,146`; `docs/OPEN_WORK_CHECKLIST.md:73`; `docs/DOCS_INDEX.md:72`; `docs/testing/TEST_COVERAGE_MATRIX.md`; `docs/architecture/LOCAL_API_EVIDENCE_MUTATION_MATRIX.md`; `docs/architecture/CONFIG_KEY_GLOSSARY.md:214`; `docs/archive/docs-housekeeping/2026-07-04-doc-cleanup/architecture/NETWORK_WORKER_HARDENING_PLAN.md:3,40,55`; `src/mediapipeline/core/network/facade.py:392` | Active docs and read text mix design-only/read-only/absent statements with the current backend-owned lifecycle/setup routes, making validation planning unreliable. | Current contract exposes dry-run/start/stop/test-connection/discovery/join routes; active docs still mention design-only fields (`would_write_state`), absent controls, stale route totals, and applied `WorkerConfigOverrides`. | Refresh active docs, matrix rows, route totals, hardening plan status, field names, and read DTO wording to distinguish read panels from backend-owned lifecycle/setup commands. | Run route inventory tests, API contract payload tests, WebView route boundary tests, and grep active docs for stale absent/design-only phrases. |
| NCW-P2-17 | P2 | join blob hardening/tests | W09,W12 | `src/mediapipeline/core/network/join.py:39-89`, `:145-161`; `tests/python/desktop/test_network_join.py` | Secret-bearing join blob failure paths are under-tested and helper-level semantic size/row/field caps are absent. | Local API body cap exists, but `join.py` decodes whole blobs, copies all valid-looking library rows, and serializes all mappings; tests cover happy path and missing confirmation, not hostile malformed blobs. | Add conservative encoded/decoded length, library row count, and field length caps; keep errors redacted and fail before settings save. | Add facade and Local API tests for invalid base64, wrong schema, too-short token, oversized/hostile blobs, no partial save, no journal leak. |
| NCW-P2-18 | P2 | PowerShell/Python result contract | W13 | `ops/pipeline/engine/process/worker_result.ps1:40-81`; `pipeline_processing.ps1`; `protocol.py:189-195` | Even after a result bridge exists, `local_worker_result.v1` lacks `QueueTerminal` and `Retryable`, so network retry semantics cannot be faithfully populated. | Process results carry `QueueTerminal`/`Retryable`; worker result payload serializes route/publish/output but omits terminal/retry fields. | Add backward-compatible optional `QueueTerminal` and `Retryable` fields and map them explicitly into Python `DoneRequest`. | Extend PowerShell result tests and add fixture-based PowerShell-result-to-Python-done mapping tests. |
| NCW-P2-19 | P2 | local worker diagnostics | W13 | `ops/pipeline/engine/queue/worker_claim_store.ps1:207-214`; `worker_process.ps1:41-43` | Stale worker-result evidence is said to remain available but is deleted on next slot reuse. | Repair marks claim `released_stale_result`; next `Start-MediaPipelineLocalWorkerChild` removes the fixed per-slot result file before launch. | Archive or embed stale result evidence before releasing/reusing the slot. | Add slot reuse test proving stale result evidence survives after a new child start. |
| NCW-P3-01 | P3 | test reliability | W01,W04 | `tests/python/desktop/test_network_workflow.py:914`, `test_http_claim_skips_records_outside_worker_accessible_libraries` | Assigned network workflow evidence is not green, and the accessible-library claim regression is inactive. | Test stubs `_snapshot_encode_config` with an old signature while production calls it with `(worker_name, record)`. | Update the test double signature or use `*args, **kwargs` where encode details are irrelevant. | Rerun the W01/W04 assigned modules and require the accessible-library skip assertions to execute. |
| NCW-P3-02 | P3 | WebView test fixture | W12 | `tests/webview/test_webview_browser_network_smoke.py:683-704`; `contract_command.py:738-759` | Browser smoke fixture marks join routes as not requiring confirmation, so it can miss UI regressions around secret/config-write confirmation cues. | Fixture sets `requires_confirmation: False` for join blob and join cluster; real Python contract requires confirmation for both. | Derive smoke fixture from `LOCAL_API_ROUTE_CONTRACT` or update fixture and add confirmation assertions. | Rerun browser Network smoke and WebView network boundary tests. |
