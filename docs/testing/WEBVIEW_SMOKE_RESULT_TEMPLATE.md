# WebView Smoke Result Log Template

Copy this template after any `Test-WebView*.ps1` run and fill in the fields. Do not skip the Mutation Boundary section — incomplete records make it harder to interpret a later failure.

---

## Run Identity

| Field | Value |
|---|---|
| Date / Time (local) | |
| Operator | |
| Machine | |
| Smoke wrapper run | e.g. `Test-WebViewBrowserHighRiskSmoke.ps1` |
| Test module (if different from wrapper) | |

---

## Environment

| Field | Value |
|---|---|
| OS | Windows |
| Bundled Python path | `apps\desktop\runtime\Python\python.exe` (default) or: |
| Node.js version (`node --version`) | |
| Chrome available | Yes / No |
| Edge available | Yes / No |
| Browser used (if browser-backed) | Chrome / Edge / N/A |
| Local API port | e.g. 47391 (auto-assigned by wrapper) |
| Bootstrap token source | Auto-generated by wrapper / other: |
| Fixture root | Generated disposable path / N/A: |
| Live/operator roots supplied | Must be `No` for fixture-backed browser validation |

---

## Command Used

```powershell
# Paste the exact command run here
```

---

## Result

| Field | Value |
|---|---|
| Exit code | Canonical wrapper: 0 = assertions passed (or explicit non-gating skip with `-AllowSkippedTests`); nonzero = failure |
| Outcome | Pass / Fail / Skip (non-gating only) |
| `-AllowSkippedTests` explicitly used | Yes / No |
| Browser assertions executed | Yes / No |
| Skip reason (if any) | e.g. "No Chrome/Edge found at expected paths", "Node.js not on PATH"; never record a skip as Pass |
| Test count | e.g. "Ran 1 test in 12.3s" |
| Failures / Errors | Count and short description |

---

## Browser-Backed Smoke Specifics

Skip this section for non-browser smokes (`Test-WebViewCommandEvidenceSmoke`, `Test-WebViewRowDetailSmoke`, `Test-WebViewRenameReadinessSmoke`, `Test-WebViewSettingsLaunchPolicySmoke`, `Test-WebViewSettingsPatchEvidenceSmoke`).

| Field | Value |
|---|---|
| CDP connection established | Yes / No / N/A |
| Local API started successfully | Yes / No / N/A |
| WebView index page loaded | Yes / No / N/A |
| Browser console errors observed | Yes / No |

Browser console errors (if any):

```
(paste here)
```

---

## Mutation Boundary Confirmation

This section is required for every run. Confirm both the production boundary and the scenario-specific disposable writes. Use `docs/testing/BROWSER_SMOKE_DOES_NOT_MUTATE_MATRIX.md` as the allowlist; do not assume every smoke is globally non-mutating.

| Boundary | Confirmed / evidence |
|---|---|
| No operator/live config, runtime state, media, output, scratch, manifest, sidecar, or process was read or mutated | [ ] |
| Generated disposable root recorded above | [ ] |
| Source-like media/subtitle/sidecar/manifest fixture hashes unchanged | [ ] / N/A |
| Allowed temporary config/app-state writes | List exact paths or N/A |
| Allowed temporary queue/command-journal/completed-manifest/sidecar writes | List exact paths or N/A |
| Allowed temporary lifecycle reconciliation/archive writes | List exact paths or N/A |
| Allowed screenshot/audit manifest writes | List exact evidence directory or N/A |
| No real FFmpeg/remux/encode or pending-publish drain ran | [ ] |
| No rename apply/undo ran | [ ] |
| No undeclared route or filesystem mutation occurred | [ ] |

---

## Raw Output / Failure Traceback

```
(paste relevant test output here)
```

---

## Follow-Up Notes

| Issue | Action needed | Owner |
|---|---|---|
| | | |

---

## Smoke Tier Reference

| Tier | Wrappers/modules |
|---|---|
| Non-browser JS/Node (no Chrome/Edge needed) | 8 wrappers; see the smoke catalog. `Test-WebViewSettingsLaunchLiveConfigSmoke.ps1` reads selected saved config and is not a disposable-root fixture unless explicitly redirected. |
| Browser-backed | 24 canonical wrappers / 26 Python modules. Canonical wrappers fail prerequisite skips unless `-AllowSkippedTests` is explicit. |
| Browser wrapper names | `Test-WebViewBrowserCompletedPendingProofSmoke.ps1`, `Test-WebViewBrowserDiagnosticsHandoffSmoke.ps1`, `Test-WebViewBrowserHighRiskSmoke.ps1`, `Test-WebViewBrowserHomeLiveStateSmoke.ps1`, `Test-WebViewBrowserLargeTableSmoke.ps1`, `Test-WebViewBrowserLaunchQueueReadinessSmoke.ps1`, `Test-WebViewBrowserLayoutManagerSmoke.ps1`, `Test-WebViewBrowserLibraryProfilesSaveSmoke.ps1`, `Test-WebViewBrowserLifecycleReconciliationSmoke.ps1`, `Test-WebViewBrowserLifecycleSmoke.ps1`, `Test-WebViewBrowserMaintenanceChangeLedgerSmoke.ps1`, `Test-WebViewBrowserMaintenanceReportsSmoke.ps1`, `Test-WebViewBrowserNetworkSmoke.ps1`, `Test-WebViewBrowserPendingDrainGuardSmoke.ps1`, `Test-WebViewBrowserProseBoxAudit.ps1`, `Test-WebViewBrowserQueueFileOverridesSmoke.ps1`, `Test-WebViewBrowserQueueLaunchCompletedSmoke.ps1`, `Test-WebViewBrowserRenameSmoke.ps1`, `Test-WebViewBrowserSampleValidationSmoke.ps1`, `Test-WebViewBrowserScheduleSmoke.ps1`, `Test-WebViewBrowserSettingsFieldMatrixSmoke.ps1`, `Test-WebViewBrowserSettingsLaunchSmoke.ps1`, `Test-WebViewBrowserTelemetrySmoke.ps1`, `Test-WebViewBrowserVisualClutterScreenshots.ps1` |
| Direct-only browser modules | `test_webview_browser_metrics_degraded_state_smoke.py`, `test_webview_browser_settings_builder_flush_smoke.py` |
| Release / package self-test | `ops\scripts\release\test.ps1` |

For the full catalog with scope and limitation notes see `docs/testing/WEBVIEW_SMOKE_TEST_CATALOG.md`.

---

## What This Run Does NOT Prove

A pass proves only the assertions and mutation boundary named by that smoke. A skip proves neither behavior nor non-mutation. WebView smokes do not prove:

- FFmpeg route correctness, encode choices, or media output quality
- Subtitle OCR or SRT conversion on real files
- Audio passthrough or transcode policy on real streams
- Pending publish drain or manifest integrity on real parked outputs
- Settings persistence or config file correctness beyond the specific smoke scope
- Real-media route decisions or size-guard behavior
- Plex direct-play or streaming compatibility

For real-media validation follow `docs/sample-validation/REAL_MEDIA_PILOT_CHECKLIST.md` and record evidence in `docs/sample-validation/REAL_MEDIA_VALIDATION_EVIDENCE_TEMPLATE.md`.
